/**
 * tokony customed hook  ty
 * @param {*} param0
 * @returns
 */
export default function Onglet({ children }) {
    return (
        <div className="d-flex align-items-center justify-content-center h-100">
            {children}
        </div>
    );
}
